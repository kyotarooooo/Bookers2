<% if flash[:error].present?   %>
<%# byebug %>

	<div class="form-error">
		<%= flash[:error] %>
	</div>

<%# flash[:error]="" %>
<% end %>

<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>

<h4>title</h4>
<%= f.text_field :title %>
<h4>opinion</h4>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>
<% end %>